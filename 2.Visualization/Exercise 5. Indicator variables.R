# R program to illustrate
# sum function

# Creating Vectors
x <- c(10, 20, 30, 40)
y <- c(1.8, 2.4, 3.9)
z <- c(0, -2, 4, -6)

# Calling the sum() function
sum(x==20) #计算x中20出现的次数
sum(y)#计算y的和
sum(z)
sum(x, y, z)

